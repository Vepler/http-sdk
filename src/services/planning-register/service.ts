import { queryApplications } from './routes/query-applications';
import { getApplicationById } from './routes/get-application-by-id';
import { getMapTile } from './routes/get-map-tile';

export default {
  queryApplications,
  getApplicationById,
  getMapTile,
};
